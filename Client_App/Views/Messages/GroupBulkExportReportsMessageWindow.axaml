<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Client_App.ViewModels.Messages"
        xmlns:behaviors="clr-namespace:Client_App.Behaviors"
        mc:Ignorable="d"
        x:Class="Client_App.Views.Messages.GroupBulkExportReportsMessageWindow"
        x:CompileBindings="True"
        x:DataType="vm:GroupBulkExportReportsMessageVM"
        Title="Групповая выгрузка отчётов"
        MinWidth="520" MinHeight="560"
        d:DesignWidth="520" d:DesignHeight="560"
        behaviors:WindowScreenSizeBehavior.WidthRatio="0.35" behaviors:WindowScreenSizeBehavior.HeightRatio="0.65"
        WindowStartupLocation="Manual"
        CanResize="True">

    <Design.DataContext>
        <vm:GroupBulkExportReportsMessageVM/>
    </Design.DataContext>

    <ScrollViewer>
        <Grid RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto"
              Margin="10">

            <TextBlock Grid.Row="0"
                       TextWrapping="Wrap"
                       Margin="0,0,0,10"
                       Text="Укажите файл .xlsx со списком организаций: в 1-й колонке — рег. №, во 2-й — ОКПО. Выберите формы 1.1–1.9, период выгрузки и папку для сохранения файлов .RAODB."/>

            <Border Grid.Row="1"
                    Margin="0,0,0,8"
                    Padding="8"
                    BorderBrush="Black"
                    BorderThickness="1"
                    CornerRadius="5">
                <StackPanel Spacing="6">
                    <TextBlock Text="Файл .xlsx со списком организаций:" FontWeight="SemiBold"/>
                    <Grid ColumnDefinitions="*,Auto">
                        <TextBox Grid.Column="0"
                                 IsReadOnly="True"
                                 Text="{Binding ExcelFilePath, Mode=TwoWay}"
                                 Watermark="Не выбран"/>
                        <Button Grid.Column="1"
                                Margin="8,0,0,0"
                                Content="Обзор..."
                                Click="OnBrowseExcelClicked"
                                MinWidth="90"/>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Grid.Row="2"
                    Margin="0,0,0,8"
                    Padding="8"
                    BorderBrush="Black"
                    BorderThickness="1"
                    CornerRadius="5">
                <StackPanel Spacing="8">
                    <TextBlock Text="Формы для выгрузки:" FontWeight="SemiBold"/>
                    <Grid ColumnDefinitions="*,*"
                          RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto">
                        <TextBlock Grid.Row="0" Grid.Column="0" HorizontalAlignment="Right" Margin="0,0,8,4" Text="Выбрать все:"/>
                        <CheckBox Grid.Row="0" Grid.Column="1" Name="AllForms"
                                  IsThreeState="True"
                                  Checked="AllFormsCheckBox_Checked"
                                  Unchecked="AllFormsCheckBox_Unchecked"
                                  IsChecked="{Binding CheckAllForms, Mode=TwoWay}"/>

                        <CheckBox Grid.Row="1" Grid.Column="0" Click="AnyFormCheckBox_Clicked" Content="1.1" IsChecked="{Binding CheckForm11, Mode=TwoWay}"/>
                        <CheckBox Grid.Row="1" Grid.Column="1" Click="AnyFormCheckBox_Clicked" Content="1.2" IsChecked="{Binding CheckForm12, Mode=TwoWay}"/>
                        <CheckBox Grid.Row="2" Grid.Column="0" Click="AnyFormCheckBox_Clicked" Content="1.3" IsChecked="{Binding CheckForm13, Mode=TwoWay}"/>
                        <CheckBox Grid.Row="2" Grid.Column="1" Click="AnyFormCheckBox_Clicked" Content="1.4" IsChecked="{Binding CheckForm14, Mode=TwoWay}"/>
                        <CheckBox Grid.Row="3" Grid.Column="0" Click="AnyFormCheckBox_Clicked" Content="1.5" IsChecked="{Binding CheckForm15, Mode=TwoWay}"/>
                        <CheckBox Grid.Row="3" Grid.Column="1" Click="AnyFormCheckBox_Clicked" Content="1.6" IsChecked="{Binding CheckForm16, Mode=TwoWay}"/>
                        <CheckBox Grid.Row="4" Grid.Column="0" Click="AnyFormCheckBox_Clicked" Content="1.7" IsChecked="{Binding CheckForm17, Mode=TwoWay}"/>
                        <CheckBox Grid.Row="4" Grid.Column="1" Click="AnyFormCheckBox_Clicked" Content="1.8" IsChecked="{Binding CheckForm18, Mode=TwoWay}"/>
                        <CheckBox Grid.Row="5" Grid.Column="0" Click="AnyFormCheckBox_Clicked" Content="1.9" IsChecked="{Binding CheckForm19, Mode=TwoWay}"/>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Grid.Row="3"
                    Margin="0,0,0,8"
                    Padding="8"
                    BorderBrush="Black"
                    BorderThickness="1"
                    CornerRadius="5">
                <StackPanel Spacing="8">
                    <TextBlock Text="Период выгрузки (даты начала и окончания отчёта):" FontWeight="SemiBold" TextWrapping="Wrap"/>
                    <TextBlock TextWrapping="Wrap"
                               FontSize="11"
                               Opacity="0.85"
                               Text="Если оставить поля пустыми, выгрузка будет выполнена без ограничения по периоду. Отчёт включается, если его период хотя бы частично пересекается с указанным."/>
                    <Grid ColumnDefinitions="*,Auto,*">
                        <CalendarDatePicker Grid.Column="0"
                                            Watermark="дд.мм.гг"
                                            Width="150"
                                            HorizontalAlignment="Stretch"
                                            SelectedDate="{Binding InitialDate, Mode=TwoWay}"/>
                        <TextBlock Grid.Column="1"
                                   Margin="8,0"
                                   VerticalAlignment="Center"
                                   Text="－"/>
                        <CalendarDatePicker Grid.Column="2"
                                            Watermark="дд.мм.гг"
                                            Width="150"
                                            HorizontalAlignment="Stretch"
                                            SelectedDate="{Binding ResidualDate, Mode=TwoWay}"/>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Grid.Row="4"
                    Margin="0,0,0,12"
                    Padding="8"
                    BorderBrush="Black"
                    BorderThickness="1"
                    CornerRadius="5">
                <StackPanel Spacing="6">
                    <TextBlock Text="Папка для сохранения .RAODB:" FontWeight="SemiBold"/>
                    <Grid ColumnDefinitions="*,Auto">
                        <TextBox Grid.Column="0"
                                 IsReadOnly="True"
                                 Text="{Binding OutputFolderPath, Mode=TwoWay}"
                                 Watermark="Не выбрана"/>
                        <Button Grid.Column="1"
                                Margin="8,0,0,0"
                                Content="Обзор..."
                                Click="OnBrowseFolderClicked"
                                MinWidth="90"/>
                    </Grid>
                </StackPanel>
            </Border>

            <StackPanel Grid.Row="5"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Spacing="16">
                <Button Content="Запуск"
                        IsDefault="True"
                        Width="100"
                        Click="OnOkButtonClicked"/>
                <Button Content="Отмена"
                        IsCancel="True"
                        Width="100"
                        Click="OnCancelButtonClicked"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Window>
